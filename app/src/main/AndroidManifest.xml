<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aqua_society.movieprank">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_1" />
        <activity android:name=".Activity_2" />
        <activity
            android:name=".Activity_player"
            android:configChanges="keyboardHidden|orientation|screenSize" />

        <!-- Service that delivers messages from the phone "quick response" -->
        <activity android:name=".RegisterActivities.UserName" />
        <activity android:name=".RegisterActivities.UserTele" />
        <activity android:name=".RegisterActivities.UserEmail" />
        <activity android:name=".Activation"></activity>
    </application>

</manifest>